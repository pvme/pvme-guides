name: Check Emoji Spacing

on:
  pull_request:

jobs:
  check-spacing:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Ensures HEAD~1 is available

      - name: Scan changed files for improperly spaced emojis
        id: grep
        run: |
          echo "Getting list of changed files..."
          changed_files=$(git diff --name-only HEAD~1 | grep -E '\.(md|txt|js|ts|py|html|rb|go|c|cpp)$' || true)

          echo "Changed files:"
          echo "$changed_files"

          bad=""
          for file in $changed_files; do
            while IFS= read -r line; do
              bad+="$file:$line"$'\n'
            done < <(grep -nRP '\S<:[a-zA-Z0-9_]+:[0-9]+>|<:[a-zA-Z0-9_]+:[0-9]+>\S' "$file" || true)
          done

          if [[ -n "$bad" ]]; then
            echo "found=true" >> "$GITHUB_OUTPUT"
            printf "result<<EOF\n%s\nEOF\n" "$bad" >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Post new comment if issues found
        if: steps.grep.outputs.found == 'true'
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## ❌ Style Guide issue: Emoji Spacing

            One or more lines contain a Discord-style emoji without spacing around it.

            💡 Emojis like `<:emoji:123>` must have a space **before and after** them.

            **Offending lines:**
            ```
            ${{ steps.grep.outputs.result }}
            ```

            ✅ Please fix the spacing before continuing.

      - name: Fail check if issues found
        if: steps.grep.outputs.found == 'true'
        run: exit 1
